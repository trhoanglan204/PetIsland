@model IEnumerable<PetCategoryModel>

@{
    ViewData["title"] = "List Category";

    var pager = new Paginate();

    int pageNo = 0;

    if (ViewBag.Pager != null)
    {
        pager = ViewBag.Pager;

        pageNo = pager.CurrentPage;
    }

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <h3>List Category</h3>

    <a class="btn btn-primary" asp-action="Create" asp-controller="PetCategory">Add Category</a>

    <table class="table" id="myTable">
        <thead>
            <tr>
                <th>Id</th>
                <th>Category Name</th>
                <th>Category Slug</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td> @item.Slug </td>
                    <td>
                        <a class="btn btn-warning btn-sm" asp-action="Edit" asp-controller="PetCategory" asp-route-Id="@item.Id"><i class="bi bi-pencil-square"></i>Edit</a>
                        <a class="btn btn-danger btn-sm" asp-action="Delete" asp-controller="PetCategory" asp-route-Id="@item.Id"><i class="bi bi-trash-fill"></i>Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <partial name="_Paging" model="@pager" />

</body>
</html>
