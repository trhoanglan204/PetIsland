@{
    ViewData["Title"] = "Truy cập bị từ chối";
}

<div class="container text-center mt-5">
    <h1 class="text-danger">Truy cập bị từ chối</h1>
    <p class="lead mt-3">
        Bạn không có quyền truy cập vào trang này hoặc tài nguyên này.
    </p>
    <p>
        Vui lòng kiểm tra lại quyền của bạn hoặc liên hệ với quản trị viên nếu bạn nghĩ đây là lỗi.
    </p>
    <div class="mt-4">
        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Quay về trang chủ</a>
        <a asp-controller="Account" asp-action="Login" class="btn btn-outline-secondary">Đăng nhập tài khoản khác</a>
    </div>
</div>

@section Styles {
    <style>
        .container {
            max-width: 600px;
        }

        .text-danger {
            font-size: 2.5rem;
            font-weight: bold;
        }

        .lead {
            font-size: 1.25rem;
            color: #555;
        }

        .btn {
            margin: 0 10px;
        }
    </style>
}